enum Role {
    ADMIN
    TECHNICIAN
    CUSTOMER
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum Gender {
    MALE
    FEMALE
    OTHER
}

enum AuthProvider {
    CREDENTIAL
    GOOGLE
}

enum PaymentStatus {
    UNPAID
    PAID
    FAILED
    CANCELLED
    REFUNDED
}

enum VendorStatus {
    PENDING
    APPROVED
    SUSPENDED
}
